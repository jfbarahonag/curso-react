import { Component } from "react";

class Header extends Component{
    render() {
        return(
            <section>
                <h1>Velas decoradas</h1>
            </section>
        )
    }
}

export default Header;