import { Component } from "react";

class Header extends Component{
    render() {
        return(
            <section>
                <h4>Velas decoradas</h4>
            </section>
        )
    }
}

export default Header;